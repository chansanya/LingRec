# AGENTS.md

## 通用协作规范

- 默认使用简体中文回复；代码标识符、命令、日志和报错保持原始语言。
- 仅针对用户需求修改代码，不得顺手扩大业务范围或改变无关功能。
- 用户明确要求不编译或不过多测试时，只进行静态检查，并在交付说明中明确未验证项。

## Java 方法注释规范

- 新增或修改任何显式 Java 方法时，必须同步编写或更新 Javadoc；该要求覆盖 `public`、`protected`、`private` 方法和显式构造器。
- Javadoc 首段必须说明方法承担的实际业务功能、关键处理过程或适用范围，禁止仅重复方法名。
- 方法存在入参时，必须为每个参数编写 `@param`，说明业务含义，并在必要时注明是否可为空、取值范围、单位或特殊语义。
- 非 `void` 方法必须编写 `@return`，说明返回数据的内容、排序方式、数量限制以及空值或空集合语义。
- 方法存在明确异常条件时，必须编写 `@throws`，说明触发条件和异常用途。
- 重写框架方法也必须说明本项目中的实际行为，不能只写无意义的 `{@inheritDoc}`。
- 修改方法实现后必须检查原有 Javadoc 是否仍与实际逻辑一致；注释过期视为代码缺陷。
- 注释应精简准确，重点描述业务规则、边界和副作用，不为显而易见的赋值、循环或语法行为添加废话注释。

## 代理交付输出规范

- 修改 Java 代码后，最终回复必须说明是否已同步更新方法 Javadoc。
- 最终回复必须分别说明已执行的静态检查、编译检查和运行测试；未执行的项目不得暗示为已验证。
- 若仍有编译、运行、数据库或联调风险，必须明确列出并交由用户验证。
